# Base URL of the Pet Health API (same backend as apps/web).
VITE_API_URL=http://localhost:5000
# Neighbourhood of the pets, locaiton by pet, send request friendship request, accept reject requests, 2FA authentication, Email by email